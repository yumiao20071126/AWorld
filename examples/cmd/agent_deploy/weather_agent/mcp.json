{
    "mcpServers": {
        "weather-mcp-server": {
            "command": "uvx",
            "args": [
                "--from",
                "git+https://github.com/adhikasp/mcp-weather.git",
                "mcp-weather"
            ],
            "env": {
                "ACCUWEATHER_API_KEY": "${ACCUWEATHER_API_KEY}"
            }
        }
    }
}